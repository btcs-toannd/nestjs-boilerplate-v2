export enum ENVIRONMENTS {
  DEVELOPMENT = 'development',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export enum LOGGER_LEVEL {
  TRACE = 'trace',
  WARN = 'warn',
  DEBUG = 'debug',
  INFO = 'info',
  ERROR = 'error',
  FATAL = 'fatal',
  SILENT = 'silent',
}
